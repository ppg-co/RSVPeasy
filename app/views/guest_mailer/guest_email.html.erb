<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

  <body>
    <%= yield %>
    <p>Dear <%= @guest.name %></p>
    <p>You have been invited to: <%= @event.name %></p>
    <p>RSVP Here: <%= event_landing_page_url(@event)  %></p>
    <p>Use this ID to RSVP: <%= @guest.id %></p>
  
  </body>
</html>
