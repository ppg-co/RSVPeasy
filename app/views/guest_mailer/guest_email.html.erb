<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>
  <%# >Yes< %>
  <body>
    <%= yield %>
    <p>Dear <%= @guestlist.firstname %></p>
    <p>You have been invited to: <%= @event.name %> by <%= @event.hostname %> </p>
    <p>Will you be attending? </p>
    <p>Click <a href="/events/<%= @current_event.id %>/guestlists/<%= @current_guest.id %>/response_page"/a> or <a href="/events/<%= @current_event.id %>/guestlists/<%= @current_guest.id %>/response_page/No">No</a> </p>


  </body>
</html>
