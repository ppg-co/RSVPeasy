<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

  <body>
    <%= yield %>
    <p>Dear <%= @guestlist.firstname %></p>
    <p>You have been invited to: <%= @event.name %> by <%= @event.hostname %> </p>
    <p>RSVP here: <%= event_guestlist_response_page_url(@event, @guestlist) %></p>

  </body>
</html>
